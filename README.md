# Telegram AI Assistant

[![Cloudflare Workers](https://img.shields.io/badge/Cloudflare-Workers-orange)](https://workers.cloudflare.com/)
[![License](https://img.shields.io/badge/license-MIT-blue)](LICENSE)

?? **Cloudflare Workers** ? Telegram ?? AI ?????????????????????????? Token ???????????

---

## Features

- **?????** ? ?????? DeepSeek Chat / Reasoner / Qwen Flash
- **??????** ? ????????????????????????? KV??????
- **?????** ? ???? 16 ??????????? Token ????
- **Token ??** ? ?? Qwen Flash????????????????????
- **Webhook ??** ? ?? Telegram Secret Token ??????? API
- **?????** ? AI ??? 4096 ????????
- **API ??** ? ??????????????????
- **Serverless ??** ? Cloudflare Workers + KV???????

---

## Supported Models

| ???? | ?? | ??? |
|---------|------|--------|
| `flash` | Qwen Flash | ????? |
| `chat` | DeepSeek Chat | DeepSeek |
| `reason` | DeepSeek Reasoner | DeepSeek |

---

## Commands

```text
/model flash      ??? Qwen Flash????????
/model chat       ??? DeepSeek Chat
/model reason     ??? DeepSeek Reasoner
/current          ??????
/memory           ??????
/clear            ??????
```

---

## Quick Start

### 1. ????

- Cloudflare ???Workers + KV ???
- Telegram ??
- DeepSeek API Key ?/? ????? API Key

### 2. ?? Telegram Bot

? Telegram ?? `@BotFather`??? `/newbot`??? Bot Token?

### 3. ??? Cloudflare Workers

**????Dashboard ??????**

1. Cloudflare ??? ? **Workers & Pages** ? **Create** ? **Upload**
2. ?? `src/worker.js`???? Git ???????
3. ?? KV Namespace?? `wrangler.toml` ??? KV ID
4. ? Worker ? **Settings** ? **Variables and Secrets** ??????

**????wrangler CLI ??**

```bash
npm install -g wrangler
wrangler login
# ?? wrangler.toml ?? KV ID
wrangler secret put TG_TOKEN
wrangler secret put QWEN_API_KEY
wrangler secret put DEEPSEEK_API_KEY
wrangler secret put TG_SECRET_TOKEN
wrangler secret put SYSTEM_PROMPT
wrangler deploy
```

### 4. ????

? Cloudflare Worker ? **Settings ? Variables and Secrets** ????

| ??? | ?? | ?? | ?? |
|--------|------|------|------|
| `TG_TOKEN` | Secret | ? | Telegram Bot Token |
| `TG_SECRET_TOKEN` | Secret | ?? | Webhook Secret Token??????? |
| `QWEN_API_KEY` | Secret | ? | ????? API Key |
| `DEEPSEEK_API_KEY` | Secret | ? | DeepSeek API Key |
| `SYSTEM_PROMPT` | Secret | ? | AI ????? |
| `CHAT_KV` | KV Binding | ? | KV ??????????? |

### 5. ?? Telegram Webhook

? `YOUR_SECRET_TOKEN` ? Worker URL ???????

```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook?url=<WORKER_URL>&secret_token=<YOUR_SECRET_TOKEN>"
```

???

```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
```

---

## Architecture

```
Telegram User
     ?
     ?
Telegram API ??POST??? Cloudflare Worker (worker.js)
                            ?
                    ?????????????????
                    ?       ?       ?
               Qwen Flash  DeepSeek  KV Storage
               (????)   (??)    (????)
```

**???????**

1. Worker ?? Telegram Webhook ??
2. ?? `X-Telegram-Bot-Api-Secret-Token`?????
3. ???? 200 OK????????????
4. ???? KV ????????????????
5. ??????????
6. ????????????????????? KV
7. ?????????????

---

## FAQ

**????? / ???????**
?? Worker ???`wrangler tail` ? Dashboard Logs???? `TG_TOKEN` ? Webhook ?????

**?? API ????**
?? `TG_SECRET_TOKEN` ??????? Webhook URL ??? `&secret_token=xxx`?

**?????**
????? Qwen Flash???????????? `MAX_SHORT`??? 16????? `SYSTEM_PROMPT`?

**??????**
?? `worker.js` ??? `MODELS` ?????????????????????

---

## License

MIT
